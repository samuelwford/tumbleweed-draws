<p id="notice"><%= notice %></p>

<div class="box">
  <div class="title"><%= @drawing.title %></div>
  <%= @drawing.notes.html_safe %>
</div>

<div class="buttons">
  <%= link_to 'Edit', edit_drawing_path(@drawing), class: 'button is-primary' %>
  <%= link_to 'Back', drawings_path, class: 'button' %>
</div>