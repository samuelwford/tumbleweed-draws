<p>
  <div class="tags has-addons">
    <%= link_to @tag.name, tag_path(@tag), class: 'tag is-primary is-large' %>
    <span class="tag is-dark is-large"><%= @tag.taggings_count %></span>
  </div>
</p>
<div><%= render @drawings %></div>